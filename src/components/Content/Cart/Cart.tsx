import React, { FC } from 'react';
import styles from './Cart.module.scss';

const Cart: FC = (props) => {
    return (
        <div className={styles.cart}>
            Cart
        </div>
    )
}

export default Cart;
