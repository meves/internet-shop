import React, { FC } from 'react';
import styles from './Main.module.scss';

const Main: FC = (props) => {
    return (
        <div className={styles.main}>
            Main
        </div>
    )
}

export default Main;
